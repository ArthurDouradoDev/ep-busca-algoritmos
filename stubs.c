#include "estruturas.h"
#include <stdio.h>

void insere_lista(NoIndice **inicio, char *palavra, int linha) {
    // printf("DEBUG: insere_lista called with '%s' at line %d\n", palavra, linha);
}

void insere_arvore(NoIndice **raiz, char *palavra, int linha) {
    // printf("DEBUG: insere_arvore called with '%s' at line %d\n", palavra, linha);
}

NoIndice* busca_lista(NoIndice *inicio, char *palavra) {
    printf("DEBUG: busca_lista called with '%s'\n", palavra);
    return NULL;
}

NoIndice* busca_arvore(NoIndice *raiz, char *palavra) {
    printf("DEBUG: busca_arvore called with '%s'\n", palavra);
    return NULL;
}
